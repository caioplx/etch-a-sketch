# etch-a-sketch

The current project is a square that is filled in by a given user input. The user input determines how many smaller squares will be on each side of the container. When the mouse hovers over a square, it will darken slightly. This will compound until the box is completely black.

This project aims to demostrate basic javascript, html, and css skills, particularly focusing on DOM manipulation and events.
